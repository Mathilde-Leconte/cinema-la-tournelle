<form method="post" action="{{ path('app_admin_type_de_seance_delete', {'id': type_se_seance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_se_seance.id) }}">
    <button class="btn btn-inverse-danger me-2">Supprimer</button>
</form>
