{% extends 'base.back.html.twig' %}

{% block title %}Modifier Évenement{% endblock %}

{% block body %}
    <a href="{{ path('app_admin_evenement_index') }}" class="btn btn-inverse-secondary mb-3">Voir la liste</a>
    <div class="d-flex justify-content-between">
        <h1>Modifier l'Évenement</h1>
        {{ include('admin_evenement/_delete_form.html.twig') }}
    </div>

    {{ include('admin_evenement/_form.html.twig', {'button_label': 'Enregistrer'}) }}

{% endblock %}

